/*
 * app_version.h - Phien ban firmware Node.
 *
 * >>> TANG SO NAY MOI LAN BUILD DE NAP OTA <<<
 * Node gui NODE_FW_VERSION (so nguyen) qua LoRa; App/Web hien thi de biet
 * dang chay firmware nao va OTA da thanh cong chua (so version doi = OTA OK).
 */
#pragma once

#define NODE_FW_VERSION      3          /* so nguyen, bump moi ban OTA */
#define NODE_FW_VERSION_STR  "1.0.3"    /* chuoi hien thi trong log (tuy chon) */
